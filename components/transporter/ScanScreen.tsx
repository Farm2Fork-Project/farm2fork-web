"use client";

// Transporters use the same real, ledger-backed product trace as every other
// portal; this path is kept so the transporter shell's import stays stable.
export { default } from "../ScanScreen";
